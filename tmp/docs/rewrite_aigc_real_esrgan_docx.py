from __future__ import annotations

from datetime import datetime
from pathlib import Path
import shutil

from docx import Document


def set_paragraph_text(paragraph, text: str) -> None:
    if paragraph.runs:
        paragraph.runs[0].text = text
        for run in paragraph.runs[1:]:
            run.text = ""
    else:
        paragraph.add_run(text)


def main() -> None:
    base = Path.cwd()
    target = base / "新基于Real-ESRGAN的图像超分辨率系统设计与实现-论文初稿.docx"
    if not target.exists():
        raise FileNotFoundError(target)

    timestamp = datetime.now().strftime("%Y%m%d-%H%M%S")
    backup = target.with_name(f"{target.stem}.backup-{timestamp}{target.suffix}")
    shutil.copy2(target, backup)

    doc = Document(str(target))

    replacements = {
        26: "图像超分辨率的任务，是把低分辨率图像恢复成更清晰的高分辨率图像。这个方向在老照片修复、监控画面增强、移动端成像优化等场景里都有实际需求。传统插值方法实现简单、速度也快，但遇到模糊、噪声、压缩等复杂退化时，补细节的能力有限，结果常见边缘发虚、纹理缺失等问题。Real-ESRGAN属于面向真实场景的超分模型，对这类复杂输入有较好的适应性，因此本文选择它作为系统实现的核心模型。",
        27: "本文结合已有开源项目，完成了一套基于Real-ESRGAN的图像超分辨率系统。后端使用Python、PyTorch和OpenCV实现模型加载与推理，前端提供单图处理、批量处理、原图与结果同步对比、局部放大查看以及ZIP下载等功能。为了便于后续实验分析，本文还补充实现了评测脚本扩展，将Bicubic、ESRGAN、RealESRGAN_x4plus和realesr-general-x4v3纳入统一测试流程，并统计PSNR、SSIM、LPIPS三项指标。",
        28: "在Set5、Set14和BSD100三个公开数据集上的测试表明，系统能够完成稳定的超分处理与结果输出。以RealESRGAN_x4plus为例，其在三个数据集上的平均LPIPS分别为0.1718、0.2453和0.2858，均优于Bicubic的0.3470、0.4603和0.5285。与轻量模型realesr-general-x4v3相比，x4plus在感知指标上更有优势，而x4v3在PSNR和SSIM上略高一些。综合来看，本文实现的系统既能完成前后端功能，也能支撑超分模型的实验评测。",
        111: "数字图像已经广泛用于监控、移动终端、医学辅助和档案数字化等场景，但实际采集到的图像质量并不总是理想。受设备性能、传输压缩、拍摄抖动和环境噪声影响，图像常出现分辨率偏低、边缘发糊、纹理不清等问题。对这类图像，如果单纯依靠更换硬件来解决，一方面成本较高，另一方面对历史影像和已有网络图片也并不现实。因此，利用软件方法提升低清图像质量，具有比较直接的应用意义。",
        112: "图像超分辨率技术可以在原有采集条件不变的情况下，提高图像的清晰度和细节表现。对后续的检测、识别、展示等任务来说，图像质量提升往往能带来更好的使用效果。结合本课题的实现情况来看，如果把超分算法与可视化界面、结果下载和批量处理结合起来，就更接近实际使用场景，而不只是停留在模型结果展示层面。",
        114: "从问题本身看，图像超分辨率并不是一个容易求解的任务。低分辨率图像中的信息本来就少，经过模糊、下采样和噪声干扰之后，很多高频细节已经丢失。同一幅低分辨率图像，理论上可能对应多种高分辨率结果，所以模型做的并不是简单放大，而是在有限信息下尽量恢复合理细节[1]-[4]。",
        115: "近几年，基于深度学习的方法逐渐成为这一方向的主流，其中GAN类模型对纹理恢复的帮助尤其明显。和只追求像素误差最小的方法相比，这类模型更关注人眼看到的真实感，会尽量让边缘、纹理和局部结构看起来更自然[2]。也正因为如此，超分研究的关注点开始从单一的高PSNR，转向保真度、感知效果和实际可用性的综合平衡。",
        116: "Real-ESRGAN是这一类方法中较有代表性的模型。它面向真实场景的盲超分任务，也就是说，输入图像的退化方式并不预先给定。相比只考虑标准双三次下采样的传统设定，这种任务更贴近手机照片、网络图片和监控截图等真实输入。本文选用Real-ESRGAN，不只是因为它有公开权重和完整代码，更因为它适合作为毕业设计中的系统落地对象[5]-[6]。",
        119: "国外关于图像超分辨率的研究起步较早。早期方法主要围绕插值、稀疏表示和样本学习展开，这些方法在思路上较清晰，但对复杂纹理的恢复能力有限。随着深度学习的发展，Dong等将卷积神经网络用于单幅图像超分辨率，为后续方法奠定了基础[1]。之后出现的VDSR、EDSR、RCAN等模型，主要是在网络深度、残差连接和特征提取能力上持续改进。",
        120: "当PSNR和SSIM提升到一定程度后，研究者发现，高分并不一定意味着视觉效果更好。Ledig等提出SRGAN后，超分任务开始更重视纹理真实感[2]；随后ESRGAN在网络结构和损失设计上继续改进，进一步增强了感知效果[3]。从这一阶段开始，如何在数值指标和主观观感之间取得平衡，逐渐成为研究重点。",
        121: "再往后，研究方向开始从理想条件下的超分，转向真实场景下的盲超分和复杂退化建模。像监控图像、手机拍摄图像这类数据，往往同时带有模糊、噪声和压缩失真，因此只在双三次退化上训练的模型不一定适用。Real-ESRGAN通过高阶退化建模和改进判别器提高了这方面的适应性[5]-[6]。近两年，SwinIR、HAT、SeeSR等工作又从Transformer、语义先验和生成式恢复等角度继续推进了这一方向[16]-[17]。",
        123: "国内关于图像超分辨率的研究也较为活跃。一方面，有关传统方法和深度学习方法的综述工作不断完善[11]-[15][21]；另一方面，不少研究开始关注模型轻量化、复杂退化适配以及应用系统实现等问题。对本科毕业设计而言，这类研究思路也很有参考价值，因为它不只强调算法精度，还强调算法如何真正落到可运行的系统中。",
        127: "很多高指标模型默认低分辨率图像来自标准双三次下采样，但真实图像往往同时叠加模糊、噪声、压缩失真和传感器噪声[14]。在这种情况下，只按双三次退化训练的模型常会出现泛化不足，表现为PSNR下降、纹理失真或过度平滑[7]-[8]。",
        135: "本文的工作重点不是从零训练Real-ESRGAN，而是围绕现有模型完成系统设计、接口封装和实验验证。论文前半部分先梳理超分辨率和Real-ESRGAN的相关理论，说明生成器、判别器、高阶降质建模和损失函数等关键内容。",
        136: "在实现部分，本文基于Python、PyTorch和OpenCV整理了Real-ESRGAN的推理流程，并用Web方式完成前端交互界面。结合实际代码，又补充了评测模块，对Bicubic、ESRGAN、RealESRGAN_x4plus和realesr-general-x4v3进行统一测试，在Set5、Set14和BSD100上统计PSNR、SSIM与LPIPS结果。",
        137: "需要说明的是，本课题受时间和硬件条件限制，没有展开从零训练，而是以官方公开预训练权重为基础，完成模型接入、前后端联调、批处理功能和评测流程扩展。这一处理方式也更符合本次毕业设计“系统设计与实现”的定位。",
        139: "第1章为引言，主要说明课题背景、研究意义、国内外研究现状，以及当前图像超分辨率研究中仍然存在的几个实际问题。",
        140: "第2章介绍与本课题直接相关的理论基础，包括超分辨率问题特点、Real-ESRGAN的核心结构，以及PSNR、SSIM、LPIPS等评价指标。",
        141: "第3章进行系统需求分析，从可行性、功能需求和非功能需求三个方面说明系统为什么做、需要做到什么程度。",
        142: "第4章给出系统设计方案，重点说明前端交互、后端推理、批量处理和实验评测四个模块的划分方式及其业务流程。",
        143: "第5章展示系统功能实现与测试结果，包括界面功能、批量下载、评测脚本以及公开数据集上的实验结果分析。",
        144: "第6章对全文工作进行总结，并结合当前系统和实验情况，说明仍然存在的不足以及后续可以继续改进的方向。",
        157: "之所以说它难，主要是因为低分辨率图像里缺失的信息无法被直接反推回来。下采样之后，很多高频细节已经丢掉了，而同一张低分辨率图像可能对应多种看起来合理的高分辨率结果。因此，超分模型本质上是在已有信息基础上做受约束的重建，需要借助数据先验或网络学习能力来补足细节。",
        160: "Real-ESRGAN的生成器延续了ESRGAN中的RRDB结构。可以把RRDB理解为残差连接和密集连接的组合，它的作用是让网络在层数较深的情况下仍能稳定提取特征。残差路径有助于保留原始信息，密集连接便于不同层之间复用特征，因此在纹理重建任务中表现较好。",
        161: "在官方提供的几个模型中，网络规模并不完全相同。RealESRGAN_x4plus采用较大的RRDB网络，更适合通用自然图像；RealESRGAN_x4plus_anime_6B规模更小，偏向动漫场景；realesr-general-x4v3则改用了更轻量的SRVGGNetCompact，以减小推理延迟和内存占用。本文的系统同时保留了通用模型、动漫模型和轻量模型的切换入口，便于对不同使用场景进行比较。",
        166: "Real-ESRGAN和早期模型一个比较大的区别，是它在训练阶段对退化过程的考虑更复杂。传统做法通常只使用一次模糊和一次双三次下采样，而Real-ESRGAN会组合随机模糊、缩放、噪声和JPEG压缩等操作，去模拟更接近真实图片的退化过程[5]-[6]。这样做的目的，是让模型见到更丰富的低质量输入，从而减小训练数据与真实场景之间的差距。需要说明的是，这部分复杂退化主要发生在模型预训练阶段，本文系统在推理时直接调用官方权重，不再重复构造退化流程。",
        168: "在判别器部分，Real-ESRGAN没有继续沿用传统的PatchGAN，而是使用了带谱归一化的U-Net判别器[5]。这样做的好处是，判别器不只给出整图层面的真假判断，还能对局部区域提供更细的反馈，生成器据此更容易把纹理细节补得自然一些。",
        169: "谱归一化的作用，主要是限制判别器参数变化过快，减少训练中梯度不稳定的问题[5]。对于GAN模型来说，这一点很重要，因为判别器过强或过弱都会影响生成结果。原论文的设计说明，这种判别器结构有助于在真实退化场景下获得更稳定的训练效果。",
        174: "损失函数方面，Real-ESRGAN并不是只看像素误差，而是把内容损失、感知损失和对抗损失结合起来。这样做的原因很直接：如果只优化像素一致性，结果往往比较平滑；如果只强调对抗效果，又容易引入伪影。把几类损失放在一起，可以在结构正确和纹理自然之间做平衡。其总损失函数可表示为：",
        177: "其中，L1损失主要约束重建结果与目标图像在像素层面的接近程度；感知损失通过VGG特征比较生成图像和目标图像的高层表示；对抗损失则促使生成结果更接近自然图像分布。三者一起作用后，模型既不会完全追求数值指标，也不会只追求视觉刺激性的纹理。",
        192: "LPIPS可以理解为一种更偏向人眼感受的指标。它不是逐像素比较两幅图，而是先用预训练网络提取特征，再比较特征空间中的距离[8]。对于GAN类超分模型来说，LPIPS往往比PSNR和SSIM更能反映纹理是否自然，因此本文在评测时把它作为重要补充指标。",
        194: "Blau和Michaeli指出，图像复原任务里感知质量和失真指标之间往往存在权衡[7]。简单来说，如果模型一味追求更小的均方误差，输出结果容易趋向平均化，看起来就会比较平滑；如果更强调感知真实感，数值指标又不一定最高。本文后面的实验结果和这一结论基本一致：Bicubic在PSNR上占优，但LPIPS明显落后于Real-ESRGAN系列模型。",
        196: "本系统采用较轻量的前后端实现方式。前端使用HTML、CSS和JavaScript完成文件选择、模型切换、结果展示和放大预览；后端基于Python标准库中的http.server与socketserver提供接口，结合PyTorch完成模型推理，再用OpenCV处理图像编解码和对比图拼接。对毕业设计原型系统来说，这套方案依赖较少，也便于本地部署和调试。",
        198: "本章主要介绍了后续系统实现要用到的理论和技术基础。前半部分说明了超分辨率问题本身的特点，以及Real-ESRGAN在生成器、退化建模、判别器和损失函数上的几个关键设计；后半部分介绍了PSNR、SSIM、LPIPS三类评价指标，并简要说明了本文采用的Web前后端实现方式。这些内容为后续系统设计和实验分析提供了基础。",
        203: "从技术实现看，本系统采用的工具链比较成熟：后端使用Python、PyTorch和OpenCV，前端使用HTML、CSS和JavaScript，模型部分基于官方公开的Real-ESRGAN预训练权重。因为不需要重新训练网络，所以在个人电脑上也可以完成开发、调试和测试，整体实现路径比较明确。",
        207: "系统通过浏览器提供操作界面，使用者不需要额外安装客户端。上传图片、选择模型、查看结果和下载文件都可以在网页中完成。对于毕业设计原型系统来说，这种方式部署简单，也方便演示和测试。",
        209: "结合课题任务书和当前实现情况，系统需要具备以下几项核心功能：单张图像超分处理；通用、动漫和轻量模型切换；原图与结果的并排显示及分辨率标注；结果放大查看；多张图像的批量处理；单张下载以及批量ZIP下载；另外还要支持标准化评测，便于整理实验数据和绘制论文图表。",
        211: "除功能之外，系统还需要满足一些基本的非功能要求。例如界面要尽量直观，便于非专业用户操作；模块之间要保持一定解耦，后续更换模型或新增指标时不至于改动过大；批处理和下载过程要尽量稳定，避免结果丢失；同时评测模块输出的结果应便于复查和复现。",
        213: "通过这一章的分析，可以把本系统的目标概括为两部分：一是完成面向使用者的图像超分应用流程，二是完成面向论文实验的统一评测流程。后续系统设计和代码实现都围绕这两部分展开。",
        219: "系统总体上可以分为四个部分：前端交互、后端推理、批处理管理和实验评测。前端负责文件选择、模型切换、结果展示和下载；后端负责模型加载、图像推理和结果返回；批处理模块负责多文件任务和ZIP导出；评测模块负责测试集读取、LR生成、模型对比和指标统计。这样划分后，各部分职责相对清楚。",
        220: "系统运行时，用户先在网页端上传图像并选择模型，随后请求发送到后端。后端完成图像解码、模型推理、结果编码和对比图生成，再把结果图、对比图以及尺寸信息返回前端。批量处理时，系统会按顺序处理多张图，并把结果保存在本地目录和内存缓存中，方便后续统一下载。",
        223: "单图像超分模块对应的是最基本的使用流程。用户选择一张图片后，前端把文件内容和模型名称提交给后端；后端完成推理后返回超分结果、对比图和分辨率信息；前端再把原图与结果分别显示在左右区域。这个模块也是后续批处理和界面展示功能的基础。",
        225: "批量处理是在单图功能上的进一步扩展。考虑到用户可能一次处理多张图片，系统为每张图片分别完成解码、推理、保存和对比图生成，并将结果集中管理。这样一来，用户既可以逐张查看，也可以一次性打包下载全部结果，更适合演示和简单测试使用。",
        227: "对比图与可视化模块主要服务于结果观察。后端把原图和超分结果放到同一张画布中，并补充标题和分辨率标注；前端则提供弹窗查看，方便放大观察细节。这样做既便于用户比较，也方便后续在论文中展示效果图。",
        229: "评测模块单独作为离线脚本实现，不和网页流程耦合。它负责读取测试集中的HR图像、生成或复用对应的LR图像，再调用不同模型完成超分并计算PSNR、SSIM、LPIPS。最后输出逐图像结果、均值统计和柱状图，便于后续整理实验数据。",
        231: "单图像超分的业务流程比较直接：用户在网页上传图像并选择模型，前端把请求发给后端；后端完成推理和对比图生成后，把结果返回页面；页面再展示原图、超分结果和下载按钮。",
        232: "批量处理时，用户一次提交多张图像，后端按顺序逐张推理，并生成对应的结果图和对比图。处理完成后，前端显示结果列表，用户可以单独下载，也可以通过ZIP按类型一次性下载全部超分结果或全部对比图。",
        233: "评测流程则面向实验。程序先读取数据集中的Ground Truth图像，并根据设定的放大倍数生成LR输入；然后对每个模型分别执行推理，保存SR图像；接着计算PSNR、SSIM和LPIPS，并汇总成CSV和图表。整个流程固定后，不同模型之间的比较也更公平。",
        235: "本章主要说明了系统的结构划分和各模块之间的关系。结合实际代码，可以看到单图处理、批量处理、结果可视化和实验评测四部分之间是可以对应起来的，这也为后面的实现和测试章节提供了清晰线索。",
        241: "本系统的开发与测试均在本地Windows环境下完成。硬件为AMD Ryzen7 5800H处理器，操作系统为Windows 11。开发语言为Python 3.10，深度学习框架使用PyTorch，图像处理依赖OpenCV，前端采用原生HTML、CSS和JavaScript，后端则基于Python标准库中的HTTP服务能力进行实现。由于本地没有独立GPU，相关测试均在CPU环境下完成。",
        244: "后端推理模块的职责，是把前端上传的图像送入超分模型并返回结果。具体做法是：先接收图像字节流，再将其解码为OpenCV可处理的矩阵形式，然后调用Real-ESRGAN模型完成前向推理，最后把输出图像重新编码为PNG，并以Base64形式返回给前端。这样可以兼顾接口简单性和浏览器兼容性。",
        245: "为了避免每次请求都重新加载权重，系统在后端维护了一个模型缓存字典。用户切换模型时，程序会按名称构建对应网络，并在首次使用时加载权重，后续则直接复用。考虑到当前测试环境没有独立显卡，系统默认使用CPU推理；如果后续部署到带GPU的环境，只需要调整相应参数即可。",
        247: "单图像处理功能由页面和接口共同完成。前端负责读取用户选择的图片并展示预览，同时把图像内容与模型名称发送给后端；后端返回超分结果、对比图和尺寸信息后，页面再更新显示状态。这里采用Base64嵌入JSON的方式传输图像数据，主要是为了简化网页端的处理逻辑。",
        248: "对比图由后端统一生成。程序把原图和超分结果并排放置，并在合适位置写入标题和分辨率信息；如果原图尺寸需要适配画布，则使用双三次插值完成缩放。前端只接收最终结果并展示，因此页面逻辑相对简单。当前界面仍采用左右双栏布局，左侧对应原图区域，右侧对应超分结果和对比图区域，与系统初始设计保持一致。",
        252: "批量处理是在单图处理基础上增加的批任务能力。用户一次选择多张图片后，页面统一发起请求，后端按顺序完成图像解码、模型推理、结果保存和对比图生成，并把处理结果同步返回前端。同时，结果会暂存到内存和本地目录中，便于后续集中下载。",
        253: "系统提供“全部下载超分结果”和“全部下载对比图”两种批量导出方式，实际下载内容均为ZIP压缩包。后端优先从当前批处理缓存中读取文件，并在内存中动态打包后发送到浏览器。这样既减少了重复读写，也避免了逐个文件下载带来的操作负担。考虑到当前版本还没有加入自动清理机制，连续长时间运行时仍需注意缓存占用。",
        258: "为了更方便观察细节，系统增加了结果放大查看功能。对比图在后端统一排版，顶部显示标题，底部给出输入和输出分辨率；前端通过弹窗展示高分辨率图像，用户可以直接在浏览器中放大查看局部区域。对于毕业设计展示来说，这样的可视化方式已经能够满足说明效果的需要。",
        260: "评测模块由evaluate.py实现。脚本会读取高分辨率图像，按×4退化规则生成低分辨率输入并保存，再调用所选模型完成超分重建，随后计算PSNR、SSIM和LPIPS，并导出逐图像统计表与汇总图表。当前支持Bicubic、ESRGAN_x4、RealESRGAN_x4plus和realesr-general-x4v3四种方法，便于对传统基线、早期GAN模型和Real-ESRGAN系列模型做统一比较。",
        266: "从测试结果看，任务书中要求的主要功能已经基本实现，包括单图超分、结果展示、批量处理、ZIP下载和实验评测等。受CPU和内存条件限制，批量处理的建议数量仍需要控制；LPIPS计算耗时也相对更长，但在当前测试范围内能够稳定完成。",
        269: "算法测试选用了Set5、Set14和BSD100三个公开数据集。本文把各数据集的高清图像作为Ground Truth，统一放在对应的HR目录中，再按照×4放大倍率用双三次下采样生成LR输入，并保存到数据集目录下供不同模型复用。这样做可以保证所有模型使用同一批输入，便于公平比较。",
        270: "对比模型包括Bicubic、ESRGAN_x4、RealESRGAN_x4plus和realesr-general-x4v3。这样的设置既保留了传统插值基线，也包含了经典GAN超分模型和本文重点使用的Real-ESRGAN系列模型，能够较完整地反映不同方法之间的差异。",
        272: "实验采用PSNR、SSIM和LPIPS三项指标，分别对应像素误差、结构一致性和感知质量。评测时固定×4放大倍率，并在Y通道上计算PSNR与SSIM。LPIPS依赖额外的感知网络权重，首次运行会自动下载并缓存在本地。",
        284: "Bicubic在三个数据集上的PSNR都是最高的，这与测试设置直接相关。因为LR图像本身就是由HR图像经过双三次下采样得到的，所以Bicubic在像素误差上天然更占优势。不过从LPIPS来看，它与原图的感知距离明显更大，说明结果虽然数值高，但视觉上更容易发平、发糊。",
        285: "两种Real-ESRGAN模型在LPIPS上都明显好于Bicubic和ESRGAN_x4。以Set5为例，RealESRGAN_x4plus的LPIPS为0.1718，而Bicubic为0.3470，差距比较明显。与此同时，realesr-general-x4v3在PSNR和SSIM上略占优势，而x4plus的LPIPS更低，这说明轻量模型和通用模型在设计目标上并不完全相同：前者更稳一些，后者更偏向感知效果。",
        286: "ESRGAN_x4的结果整体偏低，这与它的训练假设有关。该模型更偏向较早期的GAN超分设定，对复杂退化的适应性不如Real-ESRGAN，因此在本次统一测试中没有体现出优势。本文保留这一模型，主要是为了让技术演进的对比链更完整。",
        288: "实验同时生成了PSNR、SSIM和LPIPS三张柱状图，用来展示四种模型在三个数据集上的整体差异。相比只看表格，图表更容易观察不同模型在保真度和感知质量上的变化趋势，也更便于在论文中进行结果说明。",
        298: "本章围绕系统实现和测试展开。前半部分说明了模型推理、单图处理、批量处理、结果可视化和评测模块的具体实现；后半部分给出了系统功能测试和算法测试结果。结合三个公开数据集上的实验可以看出，Real-ESRGAN系列模型在感知质量方面表现更好，而轻量模型与通用模型之间也体现出了不同的取舍。",
        301: "本文完成了一个基于Real-ESRGAN的图像超分辨率系统，并围绕这一系统完成了论文中的分析与测试工作。整个过程包括相关理论整理、需求分析、前后端实现、批量处理功能补充以及评测实验。",
        302: "在理论部分，本文重点梳理了与本项目直接相关的内容，包括超分辨率问题的基本特点、Real-ESRGAN的生成器与判别器设计、高阶退化建模思想，以及PSNR、SSIM、LPIPS三类指标的含义。通过这些内容，可以更清楚地解释后续模型选择和实验结果。",
        303: "在系统实现部分，本文没有重新训练模型，而是基于官方公开代码和预训练权重完成了工程集成。后端实现了模型加载、推理、结果返回和ZIP打包下载；前端完成了图像上传、模型切换、结果对比、局部放大和批量处理等功能。对毕业设计来说，这部分工作更能体现“系统设计与实现”的要求。",
        304: "在实验部分，本文使用Set5、Set14和BSD100三个公开数据集，对Bicubic、ESRGAN_x4、RealESRGAN_x4plus和realesr-general-x4v3进行了统一评测。结果表明，Bicubic在PSNR上更高，但LPIPS较差；Real-ESRGAN系列在感知质量上更有优势，其中x4plus的LPIPS表现更好，x4v3则在PSNR和SSIM上略高。",
        305: "从工程角度看，本文把开源的Real-ESRGAN推理能力整理成了可以直接操作的Web系统，同时补充了批量处理和实验评测流程。这些工作虽然不属于底层模型创新，但对理解模型应用方式、完成毕业设计任务和后续演示使用都具有实际价值。",
        307: "虽然本文已经完成了系统搭建和实验验证，但仍有一些地方可以继续完善。",
        308: "首先，在模型方面，本文使用的是官方预训练权重，没有结合某一具体垂直场景做再训练或微调。因此如果后续要面向医学图像、夜间监控图像或档案扫描件等数据，可以继续收集小规模数据，尝试参数高效微调方法，以提高特定场景下的效果。",
        309: "其次，在评测方面，当前实验仍以Set5、Set14和BSD100这类标准数据集为主，低分辨率输入来自统一的双三次退化。这种设置适合做基准比较，但和真实图片常见的压缩、噪声、模糊叠加情况还有差别。后续如果条件允许，可以补充RealSR、DRealSR等更贴近真实场景的数据集[22]-[23]，进一步观察模型在复杂输入下的表现。",
        310: "最后，在系统方面，当前批量处理结果会先缓存到内存和本地目录，虽然这样便于ZIP打包下载，但长时间运行时仍可能带来额外占用。后续可以继续补充缓存清理、任务队列、历史记录等功能，使系统从毕业设计原型逐步过渡到更完整的应用形态。",
        338: "毕业设计从选题到论文修改，经历了前端界面调整、后端功能补充、测试脚本扩展和实验结果整理等多个环节。这个过程中，我对图像超分辨率和Real-ESRGAN的理解比一开始更具体了，也逐渐熟悉了把开源代码整理成可运行系统的完整过程。感谢指导教师在课题推进、论文修改和实验分析中提供的帮助；也感谢Real-ESRGAN开源项目、公开数据集和相关研究工作，为本课题的实现和测试提供了基础。",
    }

    for index, text in replacements.items():
        set_paragraph_text(doc.paragraphs[index], text)

    doc.paragraphs[335].style = doc.styles["List Paragraph"]
    set_paragraph_text(
        doc.paragraphs[335],
        "Cai J, Zeng H, Yong H, et al. RealSR: Toward real-world single image super-resolution[C]//Proceedings of the IEEE/CVF International Conference on Computer Vision (ICCV). 2019: 7185-7194.",
    )
    doc.paragraphs[336].style = doc.styles["List Paragraph"]
    set_paragraph_text(
        doc.paragraphs[336],
        "Wei P, Lu H, Liu Y, et al. DRealSR: Dynamic degradation for real-world super-resolution[C]//Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR). 2025.",
    )

    try:
        doc.save(str(target))
        saved_to = target
    except PermissionError:
        alt_target = target.with_name(f"{target.stem}-AIGC优化版{target.suffix}")
        doc.save(str(alt_target))
        saved_to = alt_target

    print(f"updated: {saved_to}")
    print(f"backup: {backup}")


if __name__ == "__main__":
    main()
